```bash
az deployment group create --name azurestorage --resource-group <rg-name> --template-file <template-file>
```

```bash
az deployment group create --name azurestorage --resource-group <rg-name> --template-file <template-file> --parameters <parameter1>=<value1> <parameter2>=<value2>
```
